import 'package:flutter/material.dart';

class AppColors {
  // Primary - Orange (from icon gradient)
  static const Color primary = Color(0xFFFF6B35);
  static const Color primaryDark = Color(0xFFE85A2B);
  static const Color primaryLight = Color(0xFFFF9D7A);

  // Secondary - Lime Green (from icon gradient)
  static const Color secondary = Color(0xFFA8E063);
  static const Color secondaryDark = Color(0xFF7CB342);
  static const Color secondaryLight = Color(0xFFC8F5A0);

  // Accent - Yellow (from icon gradient)
  static const Color accent = Color(0xFFFFD93D);
  static const Color accentDark = Color(0xFFFFC107);
  static const Color accentLight = Color(0xFFFFE082);

  // Background & Surfaces - Warm tones
  static const Color background = Color(0xFFFFF8F0);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceMuted = Color(0xFFFFF5E6);
  static const Color surfaceDark = Color(0xFF0F172A);

  // Border & Divider - Orange-tinted
  static const Color border = Color(0xFFFFE0B2);
  static const Color borderStrong = Color(0xFFFFCC80);

  // Text Colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475467);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Status
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);

}

